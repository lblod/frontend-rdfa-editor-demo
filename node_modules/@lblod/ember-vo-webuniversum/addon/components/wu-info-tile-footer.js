import Component from '@ember/component';
import layout from '../templates/components/wu-info-tile-footer';

export default Component.extend({
  layout,
  tagName: 'footer',
  classNames: ['info-tile__footer']
});
