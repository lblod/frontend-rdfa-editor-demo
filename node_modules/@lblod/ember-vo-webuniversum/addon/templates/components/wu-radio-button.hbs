<input type="radio" class="radio__toggle" name="radio-buttons" disabled={{disabled}} checked={{checked}} value={{value}} onchange={{action "changeValue"}}>
<span></span>
{{#if hasBlock}}
  {{yield}}
{{else}}
  {{label}}
{{/if}}

{{#if error}}
  <div class="form__error">{{error}}</div>
{{/if}}
