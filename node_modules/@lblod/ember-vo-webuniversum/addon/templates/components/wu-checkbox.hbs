{{input
    type="checkbox"
    class="checkbox__toggle"
    name="checkbox"
    checked=checked
    disabled=disabled
    click=(action onClick)
}}
<span></span>
{{#if hasBlock}}
  {{yield}}
{{else}}
  {{label}}
{{/if}}

{{#if error}}
  <div class="form__error">{{error}}</div>
{{/if}}
