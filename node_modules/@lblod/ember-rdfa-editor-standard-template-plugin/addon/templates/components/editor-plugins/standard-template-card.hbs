<div class="modal-dialog__header">
  <p class="u-spacer--tiny">
    Wil u graag een template invoegen voor een
    <strong>{{info.label}}</strong>?
  </p>
</div>

<div class="modal-dialog__footer">
  {{#wu-button-group}}
    {{wu-button
      label="Voeg in"
      size="small"
      isNarrow=true
      onClick=(action "insert")}}
  {{/wu-button-group}}
</div>

{{yield}}
